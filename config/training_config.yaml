model:
  name: Qwen/Qwen2.5-7B-Instruct
  trust_remote_code: false
quantization:
  load_in_4bit: true
  quant_type: nf4
  compute_dtype: bfloat16
lora:
  rank: 16
  alpha: 32
  dropout: 0.05
  target_modules: [q_proj, k_proj, v_proj, o_proj]
training:
  dataset_dir: data/processed/v1.1
  output_dir: models/checkpoints-v1.1
  final_adapter_dir: models/final/adapter-v1.1
  final_merged_dir: models/final/merged-v1.1
  learning_rate: 0.0002
  per_device_train_batch_size: 1
  gradient_accumulation_steps: 8
  num_train_epochs: 3
  max_seq_length: 2048
  gradient_checkpointing: true
  save_steps: 100
  logging_steps: 10
seed: 42
